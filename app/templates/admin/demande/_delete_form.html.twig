<form method="post" action="{{ path('admin_member_delete', {'id': demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
