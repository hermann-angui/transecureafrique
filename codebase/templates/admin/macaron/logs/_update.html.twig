{% if user is defined %}
    <div>L'utilisateur : <span class="fw-bold">{{ user.FirstName ~ user.LastName }}</span> a modifié le macaron pour le véhicule (N° Vin Chassis) : <span class="fw-bold">{{ data.NumeroVinChassis }}</span> le <span class="fw-bold">{{ date('d/m/Y') }}</span></div>
{% else %}
    <div>Mise à jour le macaron pour le véhicule (N° Vin Chassis) : <span class="fw-bold">{{ data.NumeroVinChassis }}</span> créé le <span class="fw-bold">{{ date('d/m/Y') }}</span></div>
{% endif %}
